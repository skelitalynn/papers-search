# papers-search Agent 工作约定

## 项目结论

本仓库把“调研、验证、沉淀”固化为可重复执行的项目。适用于技术框架选型、2025 年及以后论文检索、竞品对比。任何 agent 开始工作前，先读本文件，再读 `docs/调研链路.md` 和 `docs/用户偏好与记忆.md`。

一次合格交付必须回答：解决什么问题、为什么采用、位于哪一层、何时重评。所有重要判断都要有可点击且可复核的证据。

## 七步调研链路

1. 需求拆解：把问题拆成可验证的具体子问题，例如选什么框架、某个机制是否真实存在。先定义范围、排除项和决策标准。
2. 证据搜集：只找官方文档、官方 GitHub、官方博客、原始论文，论文与资料限定为 2025 年及以后。排除 SEO 博客、二手媒体、star 数和无法验证的性能数字。
3. 源码验证：从 `raw.githubusercontent.com` 抓取官方源码，确认机制真实存在。例如检查 Codex `multi_agents.rs` 的 `spawn_agent`、OpenHands `resource_lock_manager.py`、DSH `architecture.md` 的 `Capability Seam`。记录仓库、文件、提交或版本、关键词和验证结果。
4. 交叉验证：对照多个官方来源，优先形成文档、源码、官方博客三角证据。能引原文就引短原文，同时保留上下文链接。
5. 整理成文档：结论优先，WHY 优先，必要时提供对照架构图。每个结论都附可点击证据链接，并明确事实、推断与建议。
6. 质量检查：每份文档的破折号不超过 2 个，扫描敏感词，检查相对链接和外部链接，统计 Mermaid 块并实测渲染。同时按 humanizer-zh 规则去除 AI 痕迹：禁止"不是……而是……"与否定式排比句式、少用双引号包裹术语、无填充短语（"值得一提""总而言之"）、无金句腔、粗体克制。
7. 提交推送：运行质量检查，查看差异，再执行 git commit 和 push。敏感文件永不提交。若用户明确要求不提交，则停止在可提交状态。

## 硬纪律

- 不编造证据：抓不到就说抓不到；禁止占位链接、禁止假 arXiv 编号；所有引用必须真实存在
- 诚实报告工具与访问限制，不夸大能力；silence on error 不可接受
- 论文与资料只看 2025 年及以后的（更早的会误导）
- 证据标准：官方文档 / 官方 GitHub 源码 / 原始论文 / 官方 case study；禁 SEO 博客、二手媒体、star 数、无法验证的性能数字
- 框架收录两条硬标准：相关度低的不收；没有开源源码的不收（不用黑盒）
- 文档风格：短、明确、结论优先、WHY 优先于 WHAT；流程图用 mermaid；破折号每份文档不超过 2 个
- 知识文档严禁含源码（只描述机制不粘贴代码实现）
- 中文撰写

## 执行约定

### 调研开始前

- 写出主问题、子问题、决策标准、时间边界和排除项。
- 检查候选是否开源，是否与问题直接相关。任一项不满足就不收录。
- 对框架分别回答：解决什么问题、为什么采用、在哪一层、何时重评。

### 证据记录

- 证据表至少包含结论、证据、来源、链接。
- 链接尽量定位到具体文档页、源码文件或论文页面，不只链接首页。
- 源码证据记录 commit SHA 或 release 版本。分支链接只能用于探索，不能作为稳定的最终证据。
- 原文引用保持短小，不脱离上下文。无法直接证实的判断标记为“推断”或“待验证”。
- 网络、权限、工具或数据受限时，写明失败动作、错误信息和未验证范围。

### 文档与代码边界

- `docs/` 和报告模板属于知识文档，不粘贴源码实现。可以写文件路径、符号名、机制说明和证据链接。
- `scripts/` 可以包含实现代码，但只使用 Python 标准库，注释和帮助文本使用中文。
- 不写会访问真实外网的自动化测试。人工执行脚本访问外网是允许的。
- Mermaid 图必须服务于架构、流程或决策理解，并在支持 Mermaid 的渲染器中实际验证。

### 完成定义

- 七步链路有可追踪产物，关键结论至少经过两类官方来源交叉验证。
- 所有框架满足相关度和开源两条门槛。
- `scripts/quality_check.py` 通过；外链已按任务风险做实际检查；Mermaid 已实测渲染。
- `git diff` 中没有密钥、令牌、个人信息、临时抓取内容或无关改动。
- 只在用户授权时 commit 和 push。本次项目初始化明确不需要 commit。

## 用户偏好摘要

- 使用中文交流，偏好直接、实用、短而明确的方案。
- 重视诚实与透明：不编造、不夸大、主动报告错误，发现问题主动告知。
- 要求系统性、多平台、多来源调研，反对依赖单一来源。
- 写作要求结论优先、WHY 优先、Mermaid 流程图、证据链接可点击。
- 每个框架只回答解决什么问题、为什么采用、在哪一层、何时重评。

## 背景记忆

本项目脱胎于 wpKnowledge 知识飞轮项目的真实调研实践，覆盖多 Agent 选型、LangGraph 与 Temporal 框架选型、竞品对比、TDD-Agent 论文证据。方法论已在实践中验证有效，papers-search 负责把它固化为可复用骨架。完整记忆见 `docs/用户偏好与记忆.md`。
